#pragma once#include ../../Sandbox_API.h

#include "../Point2f/Point2f.h"

class Sandbox_API Ball final
{
public:
	static Ball* MakeBall() { return nullptr; }

	void Update(float _deltaTime);

	const Point2f& GetPosition() const { return Position; }
	const Point2f& GetVelocity() const { return Velocity; }
	float GetMass() const { return Mass; }

private:
	Point2f Position;
	Point2f Velocity;
	float Mass;
};